/*
 * Responsibilities:
 * - App-page adapter around the shared schema-row mapper.
 * - Keeps existing import locations stable while routing behavior through shared logic.
 */

export { mapDataRuleToGridRow, mapGridRowToSchemaRow } from '../../shared/test-data/schema-row-mapper.js';
