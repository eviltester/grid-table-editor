import { faker } from '@faker-js/faker';
import { defineDomainKeywordHelpContractTests } from '../define-domain-keyword-help-contract-tests.js';
import { executeDomainKeyword } from '../../../../../../js/domain/domain-keywords.js';
import { parseKeywordInvocation } from '../../../../../../js/domain/domain-keyword-parser.js';

defineDomainKeywordHelpContractTests('airline.airplane.iataTypeCode');

describe('airline.airplane.iataTypeCode parameter validation', () => {
  test('executes airline.airplane.iataTypeCode() successfully', () => {
    const parsed = parseKeywordInvocation('airline.airplane.iataTypeCode()');

    expect(parsed.errors).toEqual([]);

    const result = executeDomainKeyword(parsed.keyword, { faker, args: parsed.args });

    expect(result).toBeDefined();
  });
});
