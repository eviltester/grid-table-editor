import '../apps/web/styles.css';

const preview = {
  parameters: {
    layout: 'fullscreen',
    controls: {
      expanded: true,
    },
    options: {
      storySort: {
        order: ['Test Data', 'Export Formats'],
      },
    },
  },
};

export default preview;
